import pygame
# -1: No player class import
pygame.init()
dis=pygame.display.set_mode((400,300))
# -1: The clock is not properly created
# -1: This line is out of place
pygame.display.update()
# Do not use different characters when typing code, as these are not recognized as parenthesis
pygame.display.set_caption（’Snake game by Intellipnat‘）
game_over=False
while not game_over:
    # -1: You are missing the colon here and are not properly checking for the event
    for event in pygame.event.get()
        print(event)  #prints out all the actions thta take place
        on the screen

        pygame.quit()
        quit()